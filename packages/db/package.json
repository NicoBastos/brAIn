{
  "name": "@brain/db",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma schema + client for brAIn (Postgres-ready, SQLite fallback)",
  "main": "index.js",
  "scripts": {
    "prisma:generate": "prisma generate --schema=prisma/schema.prisma",
    "migrate:dev": "prisma migrate dev --schema=prisma/schema.prisma",
    "studio": "prisma studio --schema=prisma/schema.prisma",
    "typecheck": "tsc --noEmit",
    "postinstall": "prisma generate --schema=prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "dotenv": "^17.2.1",
    "pg": "^8.16.3",
    "tunnel-ssh": "^5.2.0"
  },
  "devDependencies": {
    "prisma": "^6.15.0",
    "prisma-zod-generator": "^1.18.1",
    "typescript": "^5.9.2",
    "zod": "^4.1.5"
  }
}