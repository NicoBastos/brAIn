{
  "name": "@brain/features",
  "version": "0.1.0",
  "private": true,
  "description": "Feature definitions and materialized views (query helpers)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"No tests\"",
    "docker:build": "docker build -t brain/features:local ."
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}