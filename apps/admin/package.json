{
  "name": "@brain/admin",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal SSR admin for brAIn monorepo (eval runs, toggles)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "docker:build": "docker build -t brain/admin:local ."
  },
  "dependencies": {},
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}